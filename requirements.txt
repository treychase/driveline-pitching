numpy>=1.21
pandas>=1.3
matplotlib>=3.3
# Optional: only needed for the interactive HTML dashboard (dashboard_html.py)
plotly>=5.0
# C3D reader: ezc3d is preferred (fast, robust). The pure-python `c3d`
# package is used automatically as a fallback if ezc3d is unavailable.
ezc3d>=1.5
c3d>=0.5
